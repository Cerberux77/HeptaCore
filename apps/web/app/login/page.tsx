export default function LoginPage() {
  return (
    <main style={{ padding: "32px", fontFamily: "system-ui, sans-serif" }}>
      <h1>HeptaCore Login</h1>
      <p>Email/password auth is scaffolded at the schema and guard layer. Password hashing and session issuance remain pending before tenant data is exposed.</p>
    </main>
  );
}

