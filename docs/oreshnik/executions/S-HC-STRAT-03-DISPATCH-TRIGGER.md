# STRAT-03 dispatch trigger

This branch exists only to trigger and observe the GitHub Actions workflow that enters `S-HC-STRAT-03-INTAKE-TO-ACTIVE-STRATEGY` through the canonical Oreshnik `goal` flow as operator `manuel`.

It is not the product implementation branch and must not be merged. The valid implementation branch is the one returned by Oreshnik.
